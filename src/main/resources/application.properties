## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DB_NAME}?useUnicode=true&character_set_server=utf8mb4&characterEncoding=UTF-8
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
# spring.datasource.sqlScriptEncoding=UTF-8

# http
 server.port = 8084
# https
#   server.port = 8443
#   server.ssl.key-store-type=PKCS12
#   server.ssl.key-store=/opt/keystore.jks
#   server.ssl.key-store-password=vietqrbns

# mock API
mock.file.api.path=/mock/mock-apis.json

## Th�ng tin c?u h�nh cho c? s? d? li?u ngu?n (s? d?ng RestTemplate)
#source.api.url=http://source-api.com/data

# Amazon S3
aws.access.key=${AWS_ACCESS_KEY}
aws.secret.key=${AWS_SECRET_KEY}
aws.region=ap-southeast-1
aws.s3.bucket.name=${AWS_BUCKET_NAME}
aws.s3.endpoint.url=${AWS_END_POINT}

# Redis
spring.redis.host=${REDIS_HOST}
spring.redis.port=${REDIS_PORT}
spring.redis.password=${REDIS_PASSWORD}

# MQTT Config
spring.mqtt.broker=${MQTT_HOST}:${MQTT_PORT}
spring.mqtt.username=${MQTT_USERNAME}
spring.mqtt.password=${MQTT_PASSWORD}
spring.mqtt.clientId=VietQRMQTTServer

#Verify email
#ses.verified.emails=${SES.VERYFIED.EMAILS}
#
###Config in SES
#mail.sender.username=${MAIL.SENDER.USERNAME}
#mail.sender.password=${MAIL.SENDER.PASSWORD}

#spring.mail.host=email-smtp.ap-southeast-2.amazonaws.com
#spring.mail.port=587

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


## ==============JPA / HIBERNATE=================
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql = true
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force-response=true
spring.jpa.properties.hibernate.connection.characterEncoding=utf8mb4
spring.jpa.properties.hibernate.connection.CharSet=utf8mb4
spring.jpa.properties.hibernate.connection.useUnicode=true 

# UAT Config
  app.firebase-configuration-file=bns-stagging-firebase-adminsdk-59xih-c463c9ea7a.json
# Product Config
#  app.firebase-configuration-file=vietqr-product-firebase-adminsdk-zlhs7-038d463214.json

grpc.server.port=9090
grpc.server.security.enabled=false
#Concurrency
grpc.server.executor.pool.coreSize=20
grpc.server.executor.pool.maxSize=50
grpc.server.executor.pool.keepAliveMs=10000

#Circuit Breaker
resilience4j.circuitbreaker.instances.userService.failureRateThreshold=50
resilience4j.circuitbreaker.instances.userService.waitDurationInOpenState=10000
resilience4j.circuitbreaker.instances.userService.permittedNumberOfCallsInHalfOpenState=3
resilience4j.circuitbreaker.instances.userService.slidingWindowSize=10

#Rate Limiting
resilience4j.ratelimiter.instances.userService.limitForPeriod=10
resilience4j.ratelimiter.instances.userService.limitRefreshPeriod=1s
resilience4j.ratelimiter.instances.userService.timeoutDuration=500ms




